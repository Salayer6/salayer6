/**
 * DEVELOPER API KEY
 * 
 * You can paste your personal Gemini API key here for development purposes.
 * This key will be used by default.
 * 
 * However, if a user provides their own API key through the settings modal,
 * their key will take precedence and be used instead of this one.
 * 
 * If you leave this string empty AND the user has not provided a key,
 * the application will prompt the user to enter their own key.
 */
export const DEVELOPER_API_KEY = '';
